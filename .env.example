# Optional: comma-separated extra browser origins for CORS (merged with server defaults).
# Include your dedicated freelancer frontend production URL and Vercel preview URLs.
# Include the admin Next.js app origin (e.g. http://localhost:3002 if you run admin on port 3002).
# Example: CORS_ORIGINS=https://freelancers.hirehelpinghand.com,https://admin.example.com
CORS_ORIGINS=
